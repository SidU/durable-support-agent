{
  "name": "durable-support-agent-functions",
  "version": "1.0.0",
  "private": true,
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run build",
    "start": "func start",
    "dev": "npm run build && func start"
  },
  "dependencies": {
    "@azure/cosmos": "^4.2.0",
    "durable-functions": "^3.1.0",
    "@azure/functions": "^4.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.5.4",
    "rimraf": "^6.0.1",
    "typescript": "^5.4.5"
  }
}
